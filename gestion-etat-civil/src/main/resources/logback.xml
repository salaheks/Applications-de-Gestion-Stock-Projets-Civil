<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Appender pour les logs applicatifs -->
    <appender name="APP_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%msg%n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
    </appender>

    <!-- Appender pour les logs techniques (dans un fichier) -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/application.log</file>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Configuration des loggers -->
    <logger name="ma.projet" level="INFO" additivity="false">
        <appender-ref ref="APP_CONSOLE" />
    </logger>

    <logger name="org.hibernate" level="WARN" additivity="false">
        <appender-ref ref="FILE" />
    </logger>

    <logger name="org.hibernate.SQL" level="WARN" additivity="false">
        <appender-ref ref="FILE" />
    </logger>

    <logger name="org.hibernate.type" level="WARN" additivity="false">
        <appender-ref ref="FILE" />
    </logger>

    <logger name="org.springframework" level="WARN" additivity="false">
        <appender-ref ref="FILE" />
    </logger>

    <!-- Root logger -->
    <root level="WARN">
        <appender-ref ref="FILE" />
    </root>
</configuration>